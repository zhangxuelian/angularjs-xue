angular.module('ui.xue.demo').controller('xueBadgeDemoCtrl', ['$scope', function ($scope) {
    var vm = $scope.vm = {
        dotConfig:{
            isDot:true,
            count:10
        }
    }
}])